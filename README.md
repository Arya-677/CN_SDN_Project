# CN_SDN_Project
Port Status Monitoring Tool using SDN (Mininet + POX/OpenFlow). A custom 2-switch, 4-host topology detects and logs port UP/DOWN events in real-time. The POX controller handles packet_in events, installs flow rules, and generates timestamped alerts on port failures with automatic recovery detection.
